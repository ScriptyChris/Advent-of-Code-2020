{
  "name": "Advent-of-Code-2020",
  "author": "ScriptyChris",
  "version": "1.0.0",
  "description": "https://adventofcode.com/2020/about",
  "keywords": [
    "advent of code",
    "puzzle"
  ],
  "license": "ISC",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "prettier --write index.js days/**/*.js",
    "start": "nodemon --exec \"npm run prettier && node index.js\"",
    "debug": "nodemon --exec \"npm run prettier && node --inspect-brk index.js\""
  },
  "devDependencies": {
    "nodemon": "^2.0.6",
    "prettier": "^2.2.1"
  },
  "dependencies": {
    "chalk": "^4.1.0",
    "glob": "^7.1.6"
  },
  "nodemonConfig": {
    "watch": [
      "./index.js",
      "./days/**/*.js"
    ]
  }
}
